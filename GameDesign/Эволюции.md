Повышают свою [[Редкости]] за счёт [[ОР]]. Каждая эволюция имеет свои требования на повышение редкости.
Значение количества [[ОР]] в каждой эволюции рассчитывается по аналогии с [[ОДНК]] для игрока
```dataviewjs
const { fieldModifier: f } = app.plugins.plugins["metadata-menu"].api;

dv.table(
    ["Название", "Реализовано"],
    await Promise.all(
        dv.pages()
            .where(p => p.ParentLink?.path === dv.current().file.path)
            .sort(p => p["Реализовано"])
            .map(async p => [
                p.file.link,
                await f(dv, p, "Реализовано")
            ])
    )
);
```